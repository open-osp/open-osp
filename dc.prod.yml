version: "3.3"

services:
  nginx:
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./conf/ssl:/ssl
      - ./conf/nginx/conf.d/prod.conf:/etc/nginx/conf.d/oscar.template
  tomcat_oscar:
    ports:
      - '8443:443'
