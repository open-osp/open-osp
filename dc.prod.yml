version: "3.3"

services:
  nginx:
    ports:
      - '8095:80'
      - '443:443'
    volumes:
      - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/oscar.template
  tomcat_oscar:
    ports:
      - '8443:443'